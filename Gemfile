source "https://rubygems.org"

# Local preview only. GitHub Pages ignores this file when it builds the site,
# so nothing here affects what gets published.

# Matches what GitHub Pages runs (Jekyll 3.10). Slower to install, but the
# safest guarantee that local output equals published output.
gem "github-pages", group: :jekyll_plugins

# Alternative: comment out the line above and uncomment this one for a faster,
# more modern local build. This site uses no plugins, so both render the same.
# gem "jekyll", "~> 4.3"

# Needed on Ruby 3.0+
gem "webrick", "~> 1.8"
